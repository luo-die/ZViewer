PORT=3333
HOST=::
NODE_ENV=development

# CORS 允许来源
# 开发环境可设置为 *；生产环境建议设置为前端域名，如 https://your-domain.com
# 多个来源用逗号分隔；设置为 false 表示不允许跨域
CORS_ORIGIN=*

# ========================
# 数据存储路径
# ========================
# 所有持久化数据统一存放在项目根目录的 config/ 文件夹下，
# 升级时仅需保留 config/ 目录即可保留全部数据（数据库、用户上传文件、头像等）。
#
# 下方环境变量通常无需手动配置，留空时使用默认值（config/ 下的子目录）。
# 仅在需要将数据存放到独立磁盘或 Docker 挂载卷时才需要显式指定。

# 数据根目录（默认：<project-root>/config/）
# CONFIG_DIR=

# SQLite 数据库文件路径（默认：<CONFIG_DIR>/dev.sqlite）
# DATABASE_URL=

# 用户上传文件根目录（默认：<CONFIG_DIR>/uploads/）
# UPLOADS_DIR=

# 用户头像存储目录（默认：<CONFIG_DIR>/uploads/avatars/）
# AVATARS_DIR=

# NMS 推流媒体临时目录（默认：<CONFIG_DIR>/media/）
# MEDIA_DIR=

# JWT 密钥（生产环境必须设置为强随机字符串，请勿使用默认 dev secret）
JWT_ACCESS_SECRET=your-access-secret-here
JWT_REFRESH_SECRET=your-refresh-secret-here

# Token 过期时间
JWT_ACCESS_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=30d

# OBS 推流服务端口
# RTMP 推流端口（OBS 连接），默认 3334
# RTMP_PORT=3334
# HTTP-FLV 拉流端口（Node Media Server 内部使用），默认 3335
# 统一端口后由后端 /live 路径反向代理到此端口，无需直接暴露
# HTTP_FLV_PORT=3335

# ========================
# 服务端转码（可选，仅「转码方式 = 服务端」时使用）
# ========================
# 需要 ffmpeg：让它出现在 PATH，或用 FFMPEG_PATH 指向可执行文件。
# FFMPEG_PATH=
# 强制指定视频编码器（默认自动探测硬件，回落 libx264）
# TRANSCODE_VIDEO_ENCODER=
# 硬件解码：默认跟随硬件编码器；auto / cuda / qsv / d3d11va / videotoolbox / vaapi / none
# TRANSCODE_HWACCEL=
# VAAPI 设备节点（仅 Linux）
# TRANSCODE_VAAPI_DEVICE=/dev/dri/renderD128
# libx264 编码线程数（默认一半核心）
# TRANSCODE_THREADS=
# 输出分辨率上限（默认不缩放，如 1080）
# TRANSCODE_MAX_HEIGHT=
# HLS 分片时长（秒，默认 4，可填 1~15）
# TRANSCODE_SEGMENT_SECONDS=


